<Window x:Class="MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Generar servidor de Minecraft" Height="300" Width="500">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="auto"/>
      <RowDefinition Height="auto"/>
      <RowDefinition Height="auto"/>
      <RowDefinition Height="auto"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="auto"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
    <Label Grid.Row="0" Grid.Column="0" Content="Nombre de la carpeta:"/>
    <TextBox Grid.Row="0" Grid.Column="1" x:Name="txtNombreCarpeta"/>
    <Label Grid.Row="1" Grid.Column="0" Content="Número de puerto:"/>
    <TextBox Grid.Row="1" Grid.Column="1" x:Name="txtNumeroPuerto"/>
    <Button Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Content="Aceptar" x:Name="btnAceptar"/>
    <Button Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Content="Cancelar" x:Name="btnCancelar"/>
  </Grid>
</Window>
